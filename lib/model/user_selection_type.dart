class UserSelectionType {
  final String icon, title, subTitle;
  final int userType,roleId;

  const UserSelectionType({
    required this.icon,
    required this.roleId,
    required this.userType,
    required this.title,
    required this.subTitle,
  });
}
